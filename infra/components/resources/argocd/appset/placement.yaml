apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: replace-me
  namespace: openshift-gitops
spec:
  clusterSets:
    - global
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions: [] # patch-me
